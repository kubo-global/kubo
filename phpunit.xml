<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" backupGlobals="false" bootstrap="vendor/autoload.php" colors="true" processIsolation="false" stopOnFailure="false" xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/10.5/phpunit.xsd" cacheDirectory=".phpunit.cache" backupStaticProperties="false">
  <testsuites>
    <testsuite name="Feature">
      <directory suffix="Test.php">./tests/Feature</directory>
    </testsuite>
  </testsuites>
  <php>
    <!-- The suite runs many DomPDF PDF generations in one long-lived process;
         raise the limit so accumulated peak memory doesn't abort the run. -->
    <ini name="memory_limit" value="512M"/>
    <env name="APP_ENV" value="testing"/>
    <env name="CACHE_DRIVER" value="array"/>
    <env name="SESSION_DRIVER" value="array"/>
    <env name="QUEUE_DRIVER" value="sync"/>
    <env name="DB_CONNECTION" value="testing"/>
    <!-- Cheap password hashing in tests — TestCase + factories hash a user per
         test; the production default (12 rounds) dominated the suite runtime. -->
    <env name="BCRYPT_ROUNDS" value="4"/>
    <!-- Don't inherit the production Kolibri facility override. Tests set
         their own school+facility id; the override would shadow it. -->
    <env name="KOLIBRI_FACILITY_ID" value=""/>
  </php>
  <source>
    <include>
      <directory suffix=".php">./app</directory>
    </include>
  </source>
</phpunit>
